const API_URL = 'https://mern-server3.onrender.com' || 'http://localhost:4000';

// since only exporting one thing, need to export as default, if not, we have to import in other file like { API_URL }
export default API_URL

